# 计划：示例列表 / 选择器按文件夹分组

目标：`viewer/` 的左侧“示例列表”和弹窗“选择示例”网格，按 `session.path` 所在文件夹分组展示，便于在 `generated/curated|perf|random` 等目录结构中快速定位。

## 验收标准

- 左侧“示例列表”按文件夹分组展示（在“Rust 生成 / 内置示例”等来源分组下继续细分）。
- 弹窗“选择示例”网格同样按文件夹分组展示。
- 网格视图下分组标题仍跨列显示，视觉层级清晰（来源分组 > 文件夹分组 > 卡片）。
- 不引入构建依赖，仍保持纯静态离线运行。

## 待办

- [x] 调研当前示例列表与弹窗渲染逻辑
- [x] 按 `path` 目录分组渲染（来源 > 文件夹 > 条目）
- [x] 增加文件夹分组样式（等宽/缩进/层级）
- [x] 基础校验：`node --check viewer/app.js`
- [x] 调整为“二级文件夹”分组：按 `generated/<dir>/...` / `examples/<dir>/...` 的 `<dir>` 分组
- [x] 优化展示层级：文件夹分组作为独立分区（标题 + 内部列表/网格），避免“看起来没变化”
- [ ] 手工验证：列表/网格/弹窗下分组与加载是否正常
